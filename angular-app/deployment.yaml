---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: angular-app
  namespace: angular-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: angular-app
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: angular-app
    spec:
      containers:
      - image: xueromal/angular-docker:angular-docker
        name: angular-app
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        ports:
        - containerPort: 80
          protocol: TCP
          name: angular-app
